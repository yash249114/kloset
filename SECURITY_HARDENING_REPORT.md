# SECURITY HARDENING REPORT

**Date:** 2026-06-15
**Branch:** release-candidate
**Commit:** `security: harden repository secrets handling`

---

## Executive Summary

Repository was scanned for leaked secrets, tracked sensitive files, build artifacts, and log files. 862 files were removed from git tracking, and `.gitignore` was updated to prevent future leaks.

---

## Findings

### CRITICAL — Hardcoded API Key

| File | Severity | Description |
|------|----------|-------------|
| `Kloset/frontend/.cursor/mcp.json` | **CRITICAL** | Contains plaintext API key for `@21st-dev/magic` MCP server: `093857a98149fd9e00f0daa437739b8f45d136e62be69f7c275b072c20d48aff` |

**Action:** File removed from git tracking. **Rotate this key immediately** — it has been exposed in git history.

### HIGH — Tracked Environment Files

| File | Content | Risk |
|------|---------|------|
| `frontend/.env.production` | `__PLACEHOLDER__` tokens (not real secrets) | Low — no real secrets leaked |
| `backend/.env.production` | `__PLACEHOLDER__` tokens (not real secrets) | Low — no real secrets leaked |
| `Kloset/frontend/.env.production` | `__PLACEHOLDER__` tokens | Low |
| `Kloset/backend/.env` | `your_*` placeholder values | Low |
| `Kloset/backend/.env.example` | Placeholder documentation | Safe |
| `Kloset/.env.example` | Placeholder documentation | Safe |
| `Kloset/production.env.example` | Placeholder documentation | Safe |

**Action:** All `.env.production` files removed from tracking. No real secrets were committed.

### HIGH — Hardcoded Credentials in Source

| File | Line | Value | Risk |
|------|------|-------|------|
| `artifacts/scratch/inspect_emails.go` | 24 | `password=kloset_dev_password` | Dev-only diagnostic script, not deployed |
| `Kloset/backend/internal/auth/auth_test.go` | 78 | `SecretKlosetPass!123` | Test-only, not deployed |
| `Kloset/backend/internal/config/config.go` | 115 | Default JWT: `kloset-dev-secret-change-in-production` | Fallback default; production blocks startup if unchanged |

**Action:** `inspect_emails.go` removed from tracking (entire `artifacts/` dir). Config defaults are safe — `ValidateConfig()` exits with FATAL in production if placeholder values detected.

### MEDIUM — Tracked Build Artifacts & Binaries

| Category | Count | Files |
|----------|-------|-------|
| Log files | 4 | `backend-server.log`, `backend-server-err.log`, `next-server.log`, `playwright-audit.log` |
| Go binaries | 2 | `Kloset/backend/bin/seed`, `Kloset/backend/bin/server` |
| System-generated artifacts | ~750 | `artifacts/` directory (logs, screenshots, messages, reports) |
| Lock/lint files | 2 | `skills-lock.json`, `lint-output.txt` |
| Test reports | 2 | `Kloset/frontend/public/e2e-report.json`, `Kloset/frontend/public/test-results.json` |
| Cursor config | 1 | `Kloset/frontend/.cursor/mcp.json` (contained API key) |

**Action:** All removed from git tracking.

---

## .gitignore Coverage

Updated `.gitignore` now covers:

```
.env                    # Root env
.env.local              # Local overrides
.env.production         # Production tokens
!.env.example           # Documentation templates
*.log                   # All log files
artifacts/              # System-generated artifacts
.cursor/                # Cursor IDE config
```

**Remaining tracked `.env` files (safe — documentation only):**
- `.env.example` — placeholder documentation
- `Kloset/.env.example` — placeholder documentation
- `Kloset/backend/.env.example` — placeholder documentation
- `Kloset/production.env.example` — placeholder documentation
- `production.env.example` — placeholder documentation

---

## Summary of Changes

| Action | Files Affected |
|--------|---------------|
| Removed from git (`.env.production`) | 2 |
| Removed from git (log files) | 4 |
| Removed from git (Go binaries) | 2 |
| Removed from git (artifacts/) | ~750 |
| Removed from git (lock/lint/test) | 5 |
| Removed from git (cursor config) | 1 |
| Updated `.gitignore` | 1 |
| **Total** | **~865** |

---

## Post-Remediation Checklist

- [ ] **Rotate** the 21st-dev magic API key exposed in `Kloset/frontend/.cursor/mcp.json`
- [ ] Verify `.gitignore` patterns are working (add new `.env` file, confirm `git status` ignores it)
- [ ] Consider running `git filter-branch` or BFG Repo-Cleaner to purge secrets from git history
- [ ] Enable GitHub secret scanning push protection (Settings → Code security)
- [ ] Add pre-commit hook to block `.env*` and `*.log` files

---

*Report generated by security hardening audit — 2026-06-15*
